OUT				= test.out

PROJECT			= ft_strlen

MAIN			= main.c

COMPILE			= ${PROJECT}.c

WWW				= -Wall -Wextra -Werror

all:
	gcc ${WWW} ${PATH_PROJECT}/${COMPILE} ${MAIN} -o ${OUT}

clean:
	rm -rf *.o

fclean:			clean
	rm -rf ${OUT}
